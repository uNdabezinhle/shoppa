from django.apps import AppConfig


class ProductVerifyConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.product_verify"
    label = "product_verify"
    verbose_name = "Product verification"
